# How to Build
In order to build the website, you need to have Python 3 installed on your system. The build process uses a Python script to generate the necessary HTML files from the source Markdown files.
```bash
cd src
python3 build.py
```